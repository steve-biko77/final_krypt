from django.apps import AppConfig


class BlockchainConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "contexts.blockchain"
    label = "blockchain"
    verbose_name = "Blockchain"
