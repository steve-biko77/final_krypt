{
  "name": "krypt-blockchain",
  "version": "1.0.0",
  "description": "KRYPT smart contracts (AuditTrail + Escrow) — on-chain transparency/audit layer, Polygon Amoy",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy"
  },
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "6.1.2",
    "hardhat": "^2.28.6",
    "keccak256": "^1.0.6",
    "merkletreejs": "^0.5.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^16.4.5"
  }
}
